

Run ./makeallim in cygwin or linux and the images used for fig:transformexample are generated (saved in reseps, a directory which will be generated).

After that, copy the files in reseps to the images directory, to use them for the latex manual.

